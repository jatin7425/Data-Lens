project = "Data-Lens"

[[services]]
name = "backend"
dockerfile = "Dockerfile"

# Notes:
# - Railway will build the included `Dockerfile` which installs Python and
#   dependencies and runs `uvicorn` inside the `backend` directory.
# - The repo-level `start.sh` is no longer used by Railway once a Dockerfile
#   is present; it remains in the repo for local convenience.